/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
Command: npx gltfjsx@6.5.3 public/models/Characters.glb -o src/components/Character.jsx -r public
*/

import React from 'react'
import { useGraph } from '@react-three/fiber'
import type { GroupProps } from '@react-three/fiber'
import { useGLTF, useAnimations } from '@react-three/drei'
import { SkeletonUtils } from 'three-stdlib'

export function Model(props: GroupProps): JSX.Element {
  const group = React.useRef<THREE.Group>(null)
  const { scene, animations } = useGLTF('/models/Characters.glb')
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone)
  const { actions } = useAnimations(animations, group)
  void actions; // referenced for animation setup
  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Root_Scene">
        <group name="RootNode">
          <group name="CharacterArmature" rotation={[-Math.PI / 2, 0, 0]} scale={100}>
            <primitive object={nodes.Root} />
          </group>
          <skinnedMesh name="Sam" geometry={(nodes.Sam as THREE.SkinnedMesh).geometry} material={materials.Atlas} skeleton={(nodes.Sam as THREE.SkinnedMesh).skeleton} rotation={[-Math.PI / 2, 0, 0]} scale={100} />
        </group>
      </group>
    </group>
  )
}

import * as THREE from 'three'

useGLTF.preload('/models/Characters.glb')
